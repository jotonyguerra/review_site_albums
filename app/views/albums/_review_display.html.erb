<h2> User Reviews </h2>
  <% if @album.reviews %>
    <% @album.reviews.each do |rev| %>
      <li><%= rev.rating %> <%= rev.review %>
        <% if rev.id.nil? %>
            <%= nil %>
        <% else %>
        -<font color="blue"> <%= rev.user.email %> </font></li>
        <% end %>
    <% end %>
  <%end%>
